# 테스트 설치 가이드

이 단계는 공개 배포 전에 본인 계정에서 KCMP AI 도구를 안전하게 확인하기 위한 절차입니다.

## 1. 테스트 스프레드시트 만들기

새 Google Sheets 파일을 만들고, 분석 대상 전사 자료를 별도 사본으로 준비합니다. 원본 전사본은 수정하지 않고 보관하세요.

1행은 반드시 헤더 행입니다.

| A | B | C |
| --- | --- | --- |
| 화자 | 타임스탬프 | 발화 |

실제 전사 데이터는 2행부터 넣습니다.

## 2. Apps Script 프로젝트 만들기

1. 테스트 스프레드시트에서 **확장 프로그램 → Apps Script**를 엽니다.
2. 기본 `Code.gs` 내용을 모두 지웁니다.
3. 이 저장소의 [Code.gs](./Code.gs) 전체를 붙여넣고 저장합니다.
4. 프로젝트 설정에서 **“appsscript.json 매니페스트 파일을 편집기에 표시”**를 켭니다.
5. 표시된 `appsscript.json`에 이 저장소의 [appsscript.json](./appsscript.json) 내용을 반영하고 저장합니다.

## 3. 처음 실행 및 권한 승인

1. 편집기 상단 함수 선택 목록에서 `onOpen`을 선택해 실행합니다.
2. Google 권한 승인 과정을 완료합니다.
3. 스프레드시트를 새로고침합니다.
4. 상단 메뉴에 **AI 코딩**이 나타나는지 확인합니다.
5. **AI 코딩 → 0. 시작 안내**를 열어 안내 사이드바가 보이는지 확인합니다.

요청되는 권한은 현재 스프레드시트 읽기·쓰기, 시트 UI 표시, OpenAI API 요청, 필요한 시간 기반 트리거 처리로 한정됩니다.

## 4. 개인 API 키 확인

1. OpenAI Platform에서 결제를 활성화하고 본인의 API 키를 준비합니다.
2. **AI 코딩 → 1. API키 설정**에서 입력합니다.
3. 이 키는 현재 사용자 계정에만 저장됩니다.
4. 테스트를 마치면 **AI 코딩 → 기타. 개인 API키 삭제**로 제거할 수 있습니다.

API 키를 코드·시트 셀·GitHub·문서에 붙여넣지 마세요.

## 5. 기능 점검

- 헤더 설정과 교사·학생 지정을 실행한다.
- 클러스터링과 화자별 발화분석을 실행한다.
- K, C, M, P를 순서대로 실행한다.
- 각 단계의 **이어서 코딩**은 완료 상태가 될 때까지 반복 실행한다.
- 자동 결과를 원문 전사본과 대조해 검토·수정한다.

## 다음 단계: 배포 준비

테스트가 끝나면 Apps Script 프로젝트를 표준 Google Cloud 프로젝트에 연결하고, Google Workspace Marketplace SDK에서 Editor add-on으로 테스트 배포합니다. 공개 Marketplace 게시에는 OAuth 동의 화면, 개인정보 처리 안내, 지원 연락처, 아이콘 및 Google 검토 절차가 추가됩니다.
